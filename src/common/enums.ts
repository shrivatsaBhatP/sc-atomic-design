export enum Steps {
  STEP1 = 'step1',
  STEP2 = 'step2',
  STEP3 = 'step3',
}

export enum Types {
  RECTANGLE = 'rectangle',
  SQUARE = 'square',
  CIRCLE = 'circle',
  ELLIPSE = 'ellipse',
}

export enum InputTypes {
  TEXT = 'text',
  NUMBER = 'number',
}

export enum Status {
  IDLE = 'idle',
  LOADING = 'loading',
  ERROR = 'error',
  RESET = 'reset',
  ACTIVE = 'active',
}
